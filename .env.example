# CoilFlow Environment Variables Template
# Copy this file to:
#   - .env.local (for local development)
#   - .env.production (for production deployment)
# Then update with your actual values

# ========================================
# DATABASE CONFIGURATION
# ========================================
DB_ROOT_PASSWORD=password
DB_NAME=coilflow_db
DB_USER=root
DB_PASSWORD=password

# ========================================
# BACKEND CONFIGURATION
# ========================================
NODE_ENV=development
PORT=3001

# ========================================
# FRONTEND CONFIGURATION
# ========================================
# For local: http://localhost:3001
# For production: http://your-server-ip:3001 or https://your-domain.com
VITE_API_URL=http://localhost:3001

# ========================================
# PORT SUMMARY (Conflict-Free Configuration)
# ========================================
# MySQL: Host port 3307 (avoids your existing MySQL on 3306)
# Backend: Host port 3001
# Frontend: Host port 5000 (avoids your existing 3000, 8080)
# phpMyAdmin: Host port 8090 (avoids your existing 8080)

# ========================================
# SECURITY NOTES
# ========================================
# 1. Use strong passwords for production (minimum 16 characters)
# 2. Never commit .env.local or .env.production to version control
# 3. Set file permissions: chmod 600 .env.local .env.production
# 4. Keep these files secure and backed up
